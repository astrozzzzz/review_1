#ifndef ARTIFACT_SOLVER_H
#define ARTIFACT_SOLVER_H

#include <fstream>
#include <iostream>
//FIX ME ненужный импорт
//include <string>
#include <vector>

void find_numbers(std::vector<std::vector<int>>& main_vector,
    const std::vector<int>& weights,
    int i,
    int j);


#endif  
